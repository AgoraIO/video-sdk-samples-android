pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "call_quality"
include ':app'

include ':agora_manager'
project(':agora_manager').projectDir = new File('../agora_manager')

include ':authentication_workflow'
project(':authentication_workflow').projectDir = new File('../authentication_workflow')

